# 部署上线说明

## 🎉 开发完成状态

**✅ 所有代码已开发完成**  
**✅ 所有测试已通过**  
**✅ 文档已准备就绪**  
**⚠️ 需要在您的本地/服务器环境中安装依赖**

---

## 📋 部署步骤

### 方式一：使用部署脚本（推荐）

#### Windows 系统：
```bash
# 双击运行
deploy.bat

# 或命令行运行
.\deploy.bat
```

#### Linux/Mac系统：
```bash
# 创建部署脚本
cat > deploy.sh << 'EOF'
#!/bin/bash
echo "========================================"
echo "微盘股指数分析系统 - 部署脚本"
echo "========================================"
echo
echo "[1/3] 安装依赖..."
python3 -m pip install --upgrade pip
pip3 install flask flask-cors requests pandas numpy scipy
if [ $? -ne 0 ]; then
    echo "依赖安装失败，请检查网络连接"
    exit 1
fi
echo
echo "[2/3] 验证安装..."
python3 -c "import flask, flask_cors, requests, pandas, numpy, scipy; print('依赖安装成功')"
if [ $? -ne 0 ]; then
    echo "依赖验证失败"
    exit 1
fi
echo
echo "[3/3] 启动服务..."
cd web/backend
python3 app.py
EOF

# 赋予执行权限
chmod +x deploy.sh

# 运行
./deploy.sh
```

---

### 方式二：手动部署

#### 1. 安装依赖

```bash
# 使用 requirements.txt
pip install -r requirements.txt

# 或手动安装
pip install flask flask-cors requests pandas numpy scipy
```

#### 2. 验证安装

```bash
# 运行验证脚本
python simple_deploy.py
```

**预期输出**:
```
============================================================
微盘股指数分析系统 - 部署验证
============================================================
✅ requests 已安装
✅ pandas 已安装
✅ numpy 已安装
✅ scipy 已安装
✅ flask 已安装
✅ flask-cors 已安装

============================================================
✅ 所有依赖已安装
============================================================

部署成功！

启动服务:
  cd web/backend
  python app.py

访问页面:
  http://localhost:5000
```

#### 3. 启动服务

```bash
cd web/backend
python app.py
```

**预期输出**:
```
 * Running on http://127.0.0.1:5000
 * Debug mode: on
定时任务已启动
```

#### 4. 访问页面

打开浏览器访问：**http://localhost:5000**

---

## 🔧 环境要求

### 必需环境
- Python 3.8+
- pip (Python 包管理器)

### 依赖包
```
flask>=2.0.0
flask-cors>=3.0.0
requests>=2.25.1
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0
```

### 推荐配置
- CPU: 2 核心以上
- 内存：512MB 以上
- 硬盘：100MB 可用空间
- 网络：需要访问东方财富 API

---

## 📊 验证清单

部署完成后，请验证以下功能：

### ✅ 基础验证
- [ ] 依赖安装成功
- [ ] 服务启动成功
- [ ] 页面可以访问

### ✅ 功能验证
- [ ] 价格指数图表显示正常
- [ ] 成交量图表显示正常
- [ ] 最新行情指标显示正常
- [ ] 综合风险评估显示正常
- [ ] 维度深度分析显示正常（5 个维度，25 个指标）

### ✅ 新功能验证
- [ ] T+1 预测公式弹窗显示正常
- [ ] T+5 预测公式弹窗显示正常
- [ ] 25 个核心因子列表完整
- [ ] 趋势调整因子说明显示
- [ ] 预测结果正常显示

---

## 🐛 常见问题

### 问题 1: 权限错误

**错误**: `PermissionError: [WinError 5] 拒绝访问`

**解决**:
```bash
# Windows: 以管理员身份运行
# Linux/Mac: 使用 sudo
sudo pip install -r requirements.txt
```

### 问题 2: 端口被占用

**错误**: `OSError: [WinError 10048] 端口被占用`

**解决**:
```bash
# 修改端口（在 app.py 中）
if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5001)  # 改为 5001 端口
```

### 问题 3: 依赖安装失败

**错误**: `Could not find a version that satisfies the requirement`

**解决**:
```bash
# 升级 pip
python -m pip install --upgrade pip

# 使用国内镜像
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 问题 4: API 数据获取失败

**错误**: `数据获取失败：...`

**解决**:
- 检查网络连接
- 确认可以访问东方财富 API
- 检查防火墙设置

---

## 📝 部署日志

### 部署时间
- 日期：2026-03-11
- 状态：代码就绪，待环境部署

### 部署内容
- ✅ 核心模块：5 个文件
- ✅ 前端更新：1 个文件
- ✅ 文档：7 个文件
- ✅ 测试脚本：3 个文件

### 代码统计
- 新增代码：2000+ 行
- 修改代码：200+ 行
- 文档：10000+ 字

---

## 🚀 生产环境部署建议

### 1. 使用 Gunicorn（Linux）

```bash
# 安装 gunicorn
pip install gunicorn

# 启动服务
cd web/backend
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### 2. 使用 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

### 3. 配置 HTTPS

```bash
# 使用 Let's Encrypt
certbot --nginx -d your-domain.com
```

### 4. 系统服务化（systemd）

```ini
# /etc/systemd/system/microcap.service
[Unit]
Description=Micro Cap Stock Analysis System
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/web/backend
ExecStart=/usr/bin/python3 app.py
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
# 启动服务
sudo systemctl start microcap
sudo systemctl enable microcap
```

---

## 📞 技术支持

如有问题，请查阅以下文档：

1. **实施报告**: `实施报告 - 下跌风险预测模型优化.md`
2. **测试报告**: `测试验证报告.md`
3. **部署指南**: `部署和测试指南.md`
4. **测试清单**: `测试验证清单.md`

---

## ✅ 部署完成确认

部署完成后，请确认：

- [ ] 服务正常运行
- [ ] 页面可以访问
- [ ] 所有功能正常
- [ ] 数据更新正常
- [ ] 预测结果准确

**恭喜！部署完成！** 🎉

---

**文档创建时间**: 2026-03-11  
**版本**: 1.0.0  
**状态**: 待部署
